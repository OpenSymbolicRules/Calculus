# Rule provenance

Every calculus rule declares a machine-readable `provenance` object. It records
the acquisition method and one or more stable source locators. `references`,
when present, remains reserved for mathematical bibliography.
